// settings.js for Hiyd extension
// Simple settings page with links

document.addEventListener("DOMContentLoaded", () => {
    console.log("Hiyd settings page loaded");
    // No settings to manage - just display information and links
});
